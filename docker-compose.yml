services:
  app:
    image: danielpm1982/meeting-app:v.2.0.0
    container_name: backend-springboot-springboot-container
    ports:
      - 8080:8080
    environment:
      MYSQL_PORT_3306_TCP_ADDR: mysql-server
      MYSQL_PORT_3306_TCP_PORT: 3306
      MYSQL_ENV_MYSQL_DATABASE: springboot_mysql_database
      MYSQL_ENV_MYSQL_USER: rcspell
      MYSQL_ENV_MYSQL_PASSWORD: admin
    depends_on:
      mysql:
        condition: service_healthy
  mysql:
    image: mysql:8
    container_name: meeting-app-mysql-container
    volumes:
      - /home/kali/dockerized-springboot-mysql-stack/mysql-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: springboot_mysql_database
      MYSQL_USER: rcspell
      MYSQL_PASSWORD: admin
    healthcheck:
      test: ["CMD-SHELL", 'mysqladmin ping']
      interval: 10s
      timeout: 2s
      retries: 10  
  
